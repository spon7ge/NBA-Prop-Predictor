Next steps to improve

Volatility features
- Add rolling standard deviation of PTS and MIN over last 5–10 games.
- Helps model uncertainty.
- Volatility features (std of PTS over last 5 games).
- Interaction features (e.g., MIN × TEAM_PACE).

Injury/team context
- Flag when key teammates are out (boosts usage).
- Could derive from minutes changes or an injury feed.
- Injury context (star teammates out → higher usage).

Market efficiency
- Compare your predictions to sportsbook player prop lines.
- If your RMSE vs market is lower than vs raw PTS, you’re adding real value.

Model ensembling
- Train LightGBM or XGBoost with the same features.
- Blend with CatBoost for small gains.

Distributional predictions
- Move beyond point estimates → predict quantiles or full distributions.
- Useful for betting overs/unders, not just averages.

Backtest:
For each strategy track, per bet and cumulatively:
ROI (%) = (Total profit ÷ Total amount staked) × 100
Hit rate = (Number of winning bets) ÷ (Total bets)
Volatility = Standard deviation of daily P&L
Max drawdown = Largest peak-to-trough loss
Sharpe ratio = (Average daily return ÷ Std. dev. of daily returns) × √252


make a function that grabs teams: total and spread
make a function that grabs starters for starting column