"""
NBA Points Prop Prediction Model
================================
Notebooks and scripts should import concrete submodules, e.g.:
  from src.points_model.feature_engineer.ppm_features import ppm_features
  from src.points_model.props_pipeline import min_pipeline

The full pipeline API (MinutesProjector, PointsPropModel, etc.) is not wired
in this package yet — avoid importing those from src.points_model until
the corresponding modules exist.
"""
